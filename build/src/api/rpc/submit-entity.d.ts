import SignedEntity from '../../entity/signed-entity';
export declare type SubmitEntityRequest = {
    signedEntity: SignedEntity;
};
export declare type SubmitEntityResponse = {
    signedEntity: SignedEntity;
};
